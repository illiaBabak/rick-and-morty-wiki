export const CHIPS = ['Characters', 'Locations', 'Episodes'] as const;

export const MAX_CHARACTERS_PAGES = 42;
export const MAX_LOCATIONS_PAGES = 7;
export const MAX_EPISODES_PAGES = 3;

export const OBSERVER_OPTIONS = {
  root: null,
  rootMargin: '0px',
  threshold: 0.5,
};
